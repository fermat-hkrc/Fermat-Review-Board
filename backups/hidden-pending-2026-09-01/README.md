# 暂时隐藏的 Pending 条目

此目录保存 2026-09-01 从 `content/issues/` 移出的 26 个 Pending Markdown。

- 隐藏原因和逐项复核结论见 `docs/gitcode-pending-submission-plan-2026-09-01.md`。
- 文件正文完整保留，只增加了 `hidden_from_dashboard` 和 `hidden_date` 备份字段。
- 此目录不由 Dashboard 的内容加载器读取，因此这些条目不会出现在网页列表、统计和详情页中。
- 需要恢复某一条时，将对应 Markdown 移回 `content/issues/`，并删除两个 `hidden_*` 字段。

未修改这些条目的 GitCode 状态，也没有创建或删除任何远端 Issue。
